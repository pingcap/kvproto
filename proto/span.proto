syntax = "proto3";
package span;

message Span {
    enum Event {
        Unknown = 0;
        CoprRequest = 1;
    };

    uint32 id = 1;
    oneof parent {
        bool parent_none = 2;
        uint32 parent_value = 3;
    }
    uint32 start = 4;
    uint32 end = 5;
    Event event = 6;
}
