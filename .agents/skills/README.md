# kvproto Local Skills

This directory contains repository-local skills for agents working on kvproto.

## Available skills

- `backup-meta-schema-version`: guidance for backup metadata schema updates in
  `proto/brpb.proto`, including required `BackupSchemaVersion` increment.
